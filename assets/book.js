const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];let voices=[],utter=null,music=null,osc=[];function loadVoices(){voices=speechSynthesis.getVoices()}loadVoices();speechSynthesis.onvoiceschanged=loadVoices;
function read(el){speechSynthesis.cancel();const txt=el.innerText;utter=new SpeechSynthesisUtterance(txt);utter.lang='es-ES';utter.rate=.92;utter.pitch=1;const v=voices.find(x=>/^es(-|_)/i.test(x.lang));if(v)utter.voice=v;speechSynthesis.speak(utter)}
function stopRead(){speechSynthesis.cancel()}
function startMusic(kind){stopMusic();if(kind==='off')return;const C=window.AudioContext||window.webkitAudioContext;if(!C)return;const ctx=new C(),gain=ctx.createGain();gain.gain.value=.025;gain.connect(ctx.destination);const sets={luna:[261.63,329.63,392,493.88],bruma:[220,261.63,329.63,392],brasa:[196,246.94,293.66,369.99]};let i=0;function note(){if(!music)return;const o=ctx.createOscillator(),g=ctx.createGain();o.type='sine';o.frequency.value=sets[kind][i++%4];g.gain.setValueAtTime(.0001,ctx.currentTime);g.gain.exponentialRampToValueAtTime(.28,ctx.currentTime+.5);g.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+3.6);o.connect(g);g.connect(gain);o.start();o.stop(ctx.currentTime+3.8);osc.push(o);setTimeout(note,1900)}music={ctx,gain};note()}
function stopMusic(){if(music){try{music.gain.gain.setTargetAtTime(.0001,music.ctx.currentTime,.2);setTimeout(()=>music.ctx.close(),600)}catch{}music=null}osc=[]}
$$('[data-read]').forEach(b=>b.addEventListener('click',()=>read(b.closest('.lesson'))));$$('[data-stop]').forEach(b=>b.onclick=stopRead);const sel=$('#music');if(sel)sel.onchange=()=>startMusic(sel.value);$$('[data-next]').forEach(b=>b.onclick=()=>{const card=b.closest('.lesson'),next=card.nextElementSibling;if(next?.classList.contains('lesson')){card.classList.add('flipping');setTimeout(()=>{card.classList.remove('flipping');next.scrollIntoView({behavior:'smooth',block:'center'})},620)}});const cards=$$('.lesson'), links=$$('.nav a[href^="#l"]');if('IntersectionObserver'in window){const ob=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){const id='#'+e.target.id;links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')===id))}}),{threshold:.3});cards.forEach(c=>ob.observe(c))}
